/*
Lei Ye <leiy@cs.arizona.edu>
HadoopSim is a simulator for a Hadoop Runtime by replaying the collected traces.
*/
#include "HScheduler.h"
using namespace std;

list<TaskAction> DataLocalityScheduler::assignTasks(string trackerName, long numAvailMapSlots, long numAvailReduceSlots, long now)
{
    list<TaskAction> taskAction;
    return taskAction;
}
